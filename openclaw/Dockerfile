# OpenClaw Dockerfile placeholder
# Replace this with the real OpenClaw image/build once available.

FROM python:3.11-slim

WORKDIR /app

# Install dependencies when requirements.txt is added
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt

# COPY . .

CMD ["echo", "OpenClaw agent placeholder — add your entrypoint here"]
